position: absolute

An absolute element is placed based on its nearest parent 
that has position set (not static).

If no such parent exists, it uses the screen (viewport).

It is removed from normal flow, so other elements ignore it.


🧠 Even simpler (one line)

Absolute means: “place me exactly where you want, based on my positioned parent.”

